
<% if activity.trackable %>
    added a <%= activity.trackable_type.downcase  %> <%= link_to  "'#{activity.trackable.body.truncate(20)}'" , activity.trackable.commentable %>
<% else %>
     deleted a comment
<% end %>